steps:
- name: gcr.io/cloud-builders/git
  args: ['clone', '.', 'ls', 'pwd']
- name: gcr.io/cloud-builders/gsutil
  args: ["-m", "rsync", "-r", "-c", "-d", ".", "gs:aeobucket01/dag"]
  options:
  logging: CLOUD_LOGGING_ONLY
